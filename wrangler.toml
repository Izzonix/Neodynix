name = "neodynix-pages"
compatibility_date = "2025-11-07"

# This must be TOP-LEVEL — NO [pages] section!
pages_build_output_dir = "."

# Remove node_compat — Pages Functions don't support it
# node_compat = true   ← DELETE THIS LINE

[[functions]]
  directory = "functions"
  external = [
    "@cloudflare/ai",
    "@supabase/supabase-js"
  ]

[ai]
binding = "AI"
